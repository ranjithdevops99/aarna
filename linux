imp links 
# how to reset root password in centos7 
https://www.unixmen.com/reset-root-password-centos-7/


.........................................................................................................
1.to see size of the pacakge 
  104M   du -sh nexus-professional-bundle-latest.tar.gz
2 .How to set hostname 
  hostnamectl set-hostname your-new-hostname
  hostnamectl
  /bin/bash
3. Information about the processors
  >cat /proc/cpuinfo
  >cat /proc/cpuinfo |grep proc
4. which kernel you are 
  >uname -a
5.  create a mutiple direrctories at a time 
   > mkdir /src/gitporjects -p

6. to check the internet connection in vm
    vim /etc/sysconfig/network-scripts/ifcfg-ens33
    vim /etc/resolv.conf
7. in which port service  is running
   neststat -tulpn| grep tomcat 
   ps -aux  
8. details of memory utilization,cpu etc   
    top 
9. port has been block firewall for oustide to communicate
  service firewalld stop
